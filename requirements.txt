# Runtime dependencies, pinned for reproducible builds.
# Review CVEs before bumping (Pillow and PyQt6 are the largest surfaces).
# pynput is pinned deliberately: app/hotkey.py patches its private internals
# (macOS TIS neutralization + event-tap handle capture). Re-verify on bump.
PyQt6==6.11.0
mss==10.2.0
pynput==1.8.2
Pillow==12.3.0
pytesseract==0.3.13
platformdirs==4.11.7
